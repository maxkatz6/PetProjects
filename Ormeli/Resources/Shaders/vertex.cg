
struct VerOut {
  float4 position : POSITION;
  float4 color    : COLOR;
};

VerOut main(float3 position : POSITION, float4 color : COLOR )
{	
  VerOut OUT;

  OUT.position = float4(position,1);
  OUT.color = color;

  return OUT;	
}
